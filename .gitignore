.env
__pycache__/
*.pyc
cases.json
extract_warnings.json
*.jsonl
!data/standard/*.jsonl
# R2-3 Test 跑次台账。**必须显式开例外**：上面那条 *.jsonl 是整体忽略，只给
# data/standard/ 开了口子——这个坑已经踩过三次（data/standard/syndromes.jsonl
# 差点被静默吞掉、M4 的 data/diseases.jsonl 落在例外之外），这个文件是第四次。
# 它不放 data/standard/ 是因为它不是人工整理的静态参考表，而是"Test 集被跑过
# 几次"的运行台账，必须跟它记录的那份 SDT 代码放在一起；过拟合护栏靠它说话，
# 不进版本控制就等于没有台账（换台机器、换个人就归零了）。
!eval/sdt/test_run_log.jsonl
data/graph.json
# K3a 生成物：offline/build_jieba_dict.py 的产物，随 cases.json 变化，不入版本控制
data/jieba_dict.txt
# K3b/X3 生成物
data/case_triples.jsonl
data/element_index.json
# V1 生成物
eval/epsilon.json
eval/report.json
eval/report.md
eval/mes/items.json
eval/mes/answer_key.json
eval/mes/collected.json
# M8 审计日志（哈希链 JSONL）。现在已经被上面 *.jsonl 的整体忽略规则挡住，
# 这条显式规则不是为了补一个当前缺失的忽略——是为了不让这个文件的忽略状态
# 只靠"碰巧被一条更宽的规则覆盖"。data/audit.jsonl 含 doctor_id/patient_ref/
# override_reason，是真实使用中的敏感数据；将来如果有人为了让某个新的
# .jsonl 数据文件能提交而收窄或去掉 *.jsonl 那条规则，这条独立的例外
# 能保证审计日志不会被跟着一起带进版本控制。
data/audit.jsonl
out/
.venv/
venv/
.pytest_cache/
*.egg-info/
.DS_Store

# vim 交换文件（可能含 .env 里的 key）
*.swp
*.swo
*.swn
*.swm
# 古籍原文，从 GitHub 公开仓库可下载，不入版本控制
books/
# pytest-cov 的产物
.coverage
htmlcov/
.ruff_cache/
# R8-2：本地语料里 docx 转出来的 txt 是派生物（scripts/normalize_local_corpora.py
# 幂等重生成），而且两本都是现代出版物——docx 本体是用户自己上传进版本控制的，
# 派生 txt 不再多放一份。脾胃论.txt 是原始语料（公有领域），不在这条里。
data/local_corpora/王云启医案.txt
data/local_corpora/李可医案.txt

# .env 的任何衍生文件（.bak/.old/.local…）都不能进版本控制，里面有 LLM_API_KEY。
# 原来只忽略了 `.env` 本身，`.env.bak` 是漏网的。
.env*
!.env.example

# R11 性能基准的产物。**真跑的进版本控制、假跑的不进**：真跑花了 API 钱、复现不了，
# 假跑（--backend fake / --self-test）的秒数纯属本地合成，混进仓库之后没人能从文件名
# 看出它不是真的。脚本按后端给文件名前缀，这两条就是按前缀分的。
eval/bench/consult_fake_*.json
eval/bench/startup_synthetic_*.json
