# R63 变更说明

## §1 经典方一键导入

候选改由服务端按证型/治法/病位筛（`/api/classic_formulas`），判据复用既有
实现，chip 带理由；匹配不到只给方名搜索框。剂量照带并标「原方」，一改即消，
古制不换算。「调用模板」同一套。

**第三个根因最重**：证候下拉把展示名当证型名用，157/174 个证型 label 带病名
后缀，三个端点全匹配不到：病位恒显「—」、候选恒空（同 SOURCES 第 31 条）。
加 `syndrome_name`。组成解析两词表也对不齐，带剂量条目 594→697。

## §2 拖选卡顿

`profile_drag_select.py`（真 Chromium，120 步选中 1013 字）：**长任务 0、
掉帧 0、最长帧 18ms**，改前改后同一个数——无可改。五条假设逐条排除
（无 mousemove/selectionchange 监听、术语按词包且走事件委托、hover 只改
颜色、无伪元素），钉成 9 条测试。**没动生产代码。**

脚本自己量过两次零，第二次据此改的 CSS **已回滚**（重测有无水印都是 19ms）。
请在出问题那台机器上跑它。

## §3 释义「案例不足」

`diagnose_ontology_gaps.py` 归因：**名字对不上 0、谓词写法不同 0，3068 个
空槽全是"没抽到"**，故 §3.3 第 3 步目标改归并做不到：归经 598、用量 672、
禁忌 770、炮制 790 一个未降，只能重抽。按 §3.4 只做诊断与兜底，删掉释义里
「全库缺口 598/1232 味」（R62 §7 禁统计口径），只说缺哪几项。
