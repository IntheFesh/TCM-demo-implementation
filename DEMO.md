# 演示脚本（5 分钟）

给评审/演示用的一份可执行脚本。跟 `README.md` 的区别：README 是给开发者的安装说明，
这份是给**演示者**的时间线——每一步说清楚**用哪条主诉、在哪暂停、观众此刻该注意什么、
预计多久**。

## 先决定跑哪个后端——这一条决定整个时间线

| 后端 | 一次问诊等多久 | 7 个演示点能不能塞进 5 分钟 | 代价 |
|---|---|---|---|
| **`replay`（推荐）** | 亚秒级（从磁盘读录制好的结果） | **能** | 要先在有 key 的机器上录一次（约 278 次调用）；追问和检索模式消融有限制，见下 |
| `api` | **实测 230–290s**（claude_cli 后端；触发 M2 重开方时 500s+。DeepSeek 后端的数待 AutoDL 重测） | **不能**——光第 1 点就 4–5 分钟 | 要 key、要网络，现场网络抖动会挂 |
| `local` | 取决于 GPU | 能，但要 GPU 常驻 | 起 vLLM、下基座，`scripts/start_vllm.sh` |

**「5 分钟」这个数只在 `replay` 下成立。** 用 `api` 现场真跑的话，老老实实按
「提前 15–20 分钟把要展示的几个视角各提交一次、开好几个浏览器标签页」来做，
现场只切标签页——这份脚本下面每一点都标了它需不需要一次新的真实调用。

**下面每一步给的分钟数是「讲什么、点什么」的动作时间估算**（讲解 + 点击），
不是墙钟实测。唯一实测过的等待是上表那个 230–290s。

### replay 的三条限制，演示前必须知道

回放的索引是 `(schema 名, sha256(送进模型的 system 文本))`，而 system 文本里含
**主诉原文**和**检索到的参考医案**。所以：

1. **打字跟录的差一个标点就是未命中**，`ReplayBackend` 会抛 `LLMError`（带 schema 名、
   prompt 前 100 字、sha12、已加载条数），**不会静默退回真实 API**。演示一律**复制粘贴**
   下面给的主诉原文，不要手打。
2. **追问路径在 replay 下基本用不了**：访问者答什么是现场打的，内容一变就未命中。
   所以**对外演示一律 `FAST_MODE=1`**（追问 0 轮）。这不是 bug，是内容寻址索引的必然。
3. **切检索模式也会未命中**：换模式 → 检索到的参考医案变了 → system 文本变了 → 哈希变了。
   录制清单只录了默认模式。所以第 7 点（现场消融）在 replay 下跑不了，
   替代方案见那一点。把四种模式都录一遍要 4 倍的调用量，为一个演示点不值。

## 演示前检查清单（可直接复制）

```bash
# 1. 服务起来了吗，跑的是哪个后端
curl -s localhost:8000/health | python -m json.tool
#    demo_mode 非 null = 回放模式（前端顶部会有「演示模式」小字）；null = 实时调用

# 2. 环境变量有没有上一次调试留下的残留——RETRIEVER_MODE 残留会让演示跑的
#    不是默认模式，而且不会有任何提示
env | grep -E "RETRIEVER_MODE|USE_REACT|EVAL_MODE|FAST_MODE|LLM_MODE|LORA_DIR"
#    对外演示期望：LLM_MODE=replay、FAST_MODE=1，其余最好是空的

# 3. 回放跟录制逐字节一致吗（退出码 0 才开始演示）
python -m scripts.verify_replay

# 4. 零成本的自检，顺手证明文档里的数没抄错
python -m pytest tests/ -q
python -m scripts.collect_results --check
```

**浏览器用无痕窗口。** 沉浸式翻译、Kimi 这类扩展会注入 DOM 并改写页面文字——
演示时页面上突然多出一段机翻中文，或者按钮文案被改掉，现场没法解释。
改了前端还要 `?v=N` 绕缓存（`http://localhost:8000/?v=2`），浏览器缓存住旧的
`index.html` 是这份 demo 最常见的「我明明改了啊」。

## 七个演示点

从「看得见」到「看得深」。**主诉一律复制粘贴**，理由见上面 replay 限制第 1 条。

| # | 演示点 | 主诉 | 观众此刻该注意什么 | 动作时间 | 需要新调用 |
|---|---|---|---|---|---|
| 1 | 三列集注 + 用药对照带 | A | 三列等宽无卡片；**对照带上灰的那一截是噪声地板 ε、黑的那一截才是真实分歧** | 60s | 是 |
| 2 | 六层图 + 方剂框 | A（复用第 1 点的图） | 三种边框：实线经典方 / 虚线加减方 / 点线自拟方，**一眼看出哪个是经典方** | 40s | 否 |
| 3 | 学生模式：路径高亮 | A，点「胃脘胀痛」节点 | **一个症状牵动整条推理网**（三跳到方剂层），其余降到 0.25 透明度 | 30s | 否 |
| 4 | 医生模式：处方安全闭环 | A | 见下面分步脚本——**这一点最有冲击力** | 150s | 是（切下拉会自动重跑一次） |
| 5 | 患者模式：导诊 | B | **整页换了个形态**，不是三列的裁剪版；红旗症状在首屏、不折叠；绝对没有方剂药材 | 30s | 是（切下拉会自动重跑一次） |
| 6 | 安全拦截 | C | **整页替换**、秒级返回、不出方——不是「给了方再加一句警告」 | 30s | 是（但 0 次 LLM 调用） |
| 7 | 现场消融 | A，切 dense ↔ hybrid | 同一条主诉换检索模式，**输出真的变**——图谱不是摆设 | 30s | 是（replay 下跑不了，见下） |

> **R15 起切「查看模式」会自动重新问诊一次**，不用再手点「辨证」。
> 理由：裁剪在服务端做（patient 的响应体里根本没有药名），前端切换显示等于
> 没有这个角色。切下拉之后进度条会走起来，跟手点一模一样；replay 下这一次
> 命中 fixture、零调用。

三条主诉（复制粘贴用）：

```
A: 胃脘胀痛，食后加重，嗳气泛酸，每因情志不畅而发，纳差，舌淡红苔薄白，脉弦。
B: 胸闷胸痛，冷汗
C: 胃脘疼痛数月，近日解黑色柏油样便，头晕心慌，面色苍白，倦怠乏力，舌淡，脉细数。
```

A 和 C 是 `tests/queries.txt` 的第 1、10 条；B 是专门为第 5 点加进录制清单的
（`scripts/record_fixtures.py` 的 `TRIAGE_COMPLAINT`）——它不在 queries.txt 里，
因为那 10 条都是脾胃门的辨证主诉，而导诊要演示的是另一个门类。有一条测试
（`test_demo_complaints_are_all_in_the_record_plan`）钉住这三条主诉都在录制清单里。

### 第 1 点：三列集注 + 用药对照带（60s）

粘贴主诉 A（或直接点首屏的示例 A），点「辨证」。**在结果出来之后暂停**，
先不要往下滚。

- 三列等宽等高并排，中间只有一条细线、没有卡片——这是这个 demo 的主张：
  三家不是三个独立的东西，是对**同一段主诉的三种读法**；证型/治法/方药各不
  相同，而这个不同是可以量化的
- 指对照带：共用药是实心黑点，各家独有的按各自身份色。**一眼看出共同的少、
  各自的多**——比一个 0.53 直观得多
- 指带子下面那条线：**灰的那一截是噪声地板 ε，黑的那一截才是真实分歧**。
  没跑过 `estimate_epsilon` 时显示「未测」并明说"这个数单独看没有意义"，
  不会编一个假基准
- **这里要讲透的一句**：ε 是同一条主诉、同一位医家连跑三次的用药差异，也就是
  模型自己的抖动。分歧度超过它才算「两位名医意见不同」，否则只是系统在抖。
  而且 ε 是**逐条配对**比的——这条主诉的 ε 是 0.3954（逐条那张表里的一行，
  见 `eval/RESULTS.md` 的「ε 按证型分层」一节），
  高于全局均值 0.2611 `epsilon.json:epsilon_online.mean=0.2611`（9 条里有 4 条这样）。
  拿全局均值一刀切会把它的噪声误判成分歧

### 第 2 点：六层图 + 方剂框（40s，复用第 1 点的图）

- 第二层节点 label 是「病名 · 证型」两段（如「胃痛 · 肝胃不和证」）；病名判断不了时
  只显示证型，**不显示一个悬空的「· 证型」**
- **三种边框**：实线=经典方、虚线=加减方、点线=自拟方——不用点开就看出可信度分层
- 方剂框是 compound 父节点，框里装这个方真实用的药；同一位医家的 2–3 个候选方
  各自是独立的框，**不是挤成一团**
- 划过任意一味药：tooltip 显示君臣佐使归属、剂量、以及它在这个方里的作用

### 第 3 点：学生模式路径高亮（30s，不用重新提交）

点图里的「胃脘胀痛」节点。

- 高亮亮起 **症状 → 证素 → 病名·证型 → 它牵动的全部候选方**，不是只连相邻的证素
- **这一点跟角色无关**，researcher 模式下就能看到——角色决定的是「给不给你看
  这些信息」，不是「给不给你看得更好看」
- 药材按君/臣/佐/使分行、君药加粗，模型没标 role 的归进「未标注」，**不强行归类**

### 第 4 点：医生模式处方安全闭环（150s，分步脚本）

**这是最值得现场真跑的一段。** 每步后面那句就是「观众此刻应该注意什么」。

| 步 | 动作 | 观众此刻应该注意什么 |
|---|---|---|
| 1 | 「查看模式」切到「医生（可编辑处方）」，填一个医生标识 | 医生标识是**导出时必填**的——审计日志要记是谁签发的 |
| 2 | 切下拉之后页面**自动重新问诊一次**（R15 起） | **role 是逐请求参数、裁剪在服务端**，所以换视角必须重新向后端取一次——前端自己切换显示等于没有这个角色。不是偷偷花钱：进度条会走起来，跟手点「辨证」一模一样 |
| 3 | 每位医家列的下方出现「医生编辑处方」七列表格 | 预填的是这位医家给的候选方，**不是一张空表** |
| 4 | 改一味药的剂量数字 | 约 400ms 后安全区块自动刷新——**那是防抖之后真的调了一次 `/api/prescription/validate`**，不是前端拍脑袋 |
| 5 | 点两次「+ 添加药味」，分别填 `甘草` 和 `海藻` | 这对药是十八反里最经典的配伍禁忌 |
| 6 | 等防抖 | **安全区块变红：「⚠ 配伍禁忌：甘草 反/畏 海藻」**。这条检查**不调 LLM**，是确定性规则表，毫秒级 |
| 7 | 点「导出处方」 | **导出被拒绝（422）**。拒绝发生在**服务端**，而且服务端**不信任客户端传来的任何安全判断，重新算了一遍**——前端改个变量骗不过它 |
| 8 | 在「坚持导出理由」框里填一句话，点「坚持导出」 | 系统**不阻止**医生坚持——它阻止的是「悄悄地坚持」 |
| 9 | 导出成功：药房格式文本 + 审计编号 | **那句理由跟这次导出的完整安全快照一起写进了哈希链审计日志**，这是「明知有问题仍坚持」唯一的书面记录 |

现场补一刀（可选，5s）：

```bash
python -c "from core.audit import verify_audit_chain; print(verify_audit_chain())"
```

证明这条链此刻是完整的——**审计日志被人事后改过，这里会红**。

### 第 5 点：患者模式导诊（30s）

粘贴主诉 B，点「辨证」；然后把「查看模式」切到「患者」——**页面会自动重跑一次**。

- **整个界面换了形态**，不是三列少了几栏：一整块，「可能属于 <病名> / 建议就诊
  <科室>」用 28px 宋体，下面是朱砂标题的红旗症状列表
- **红旗在首屏、不折叠**。指着说：折叠起来的急救信息等于没有
- **指着屏幕说**：这里**没有任何方剂和药材**。不是前端藏起来了——
  `formula_candidates` 这个键在响应体里**根本不存在**，打开 devtools 也看不到。
  裁剪在服务端做（连图谱都只画到证型层为止，方剂/药材那两层**压根没生成**），
  这是**安全边界，不是功能裁剪**
- 主诉 B 的紧急度是 high，所以食疗和中成药一并清空，页面上会直说
  「这类症状紧急度高，按安全规则这里不提供任何用药或食疗建议」——这条闸门
  在服务端独立生效，不是前端不画

### 第 6 点：安全拦截（30s）

粘贴主诉 C（黑便），点「辨证」。

- **秒级返回**（安全否决走在 S2 之前，0 次 LLM 调用），直接拒绝辨证
- **整页替换**，不是在三列上面加个红横幅。总纲的原话是"什么都标红会训练用户
  忽略标红"——这个系统只在真危重时打断，所以打断必须彻底：三列清空、图不画、
  对照带不画，页面上不留任何可以被误读成"结论"的东西
- **不产出任何方药**——不是「给了方再加一句警告」。这是这个 demo 里最该被记住的
  一条设计：拦截必须发生在推理开始之前。有一条 Playwright 判据钉住这件事：
  拦截之后整个问诊页的 DOM 里搜不到 `herb`/`formula`，也搜不到任何药名
- 页面上有一颗「换一条主诉」——打断彻底不等于把人困在这一页
- 可以补一句：SDT Test 上被拦的 **10/50** 条经人工逐条复核**全部为真危重，无一误伤**；
  关掉这道闸门分数从 23.173 `sdt/test_run_log.jsonl:sdt.chain_last=23.173`
  涨到 27.729 `sdt/test_run_log.jsonl:sdt.ignore_safety_veto=27.729`
  ——**那 4.56 分就是安全的代价，我们认**

### 第 7 点：现场消融（30s）

「检索模式」下拉切一个模式，重新提交主诉 A，对比检索到的参考医案有没有变。

- **replay 下跑不了**（切模式 → 参考医案变 → system 文本变 → 哈希变 → 未命中）。
  三个替代方案，挑一个：
  1. 这一点单独切到 `api` 后端跑（要网络和 key）
  2. 提前在 `api` 下把两个模式各提交一次，开两个标签页现场对照
  3. **不真跑，直接报数**：四种模式的输出差异率
     **0.437** `report_e8.json:e8.output_difference_rate=0.437`；
     旧检索层下是 0.366 `archive/2026-09-12/report_e8.json:archive.e8.output_difference_rate=0.366`——
     这个数本身就说明「换检索模式，输出真的会变」
- 这台沙箱连不上 huggingface hub，`dense`/`hybrid` 会**直接报 `retrieval_error`
  而不是静默降级**；`bm25`/`graph` 不需要网络。`graph` 要先跑过
  `python -m offline.build_element_index`，没跑过会返回 503（不是 500——
  前端能区分「数据没准备好」和「系统故障」）

## FAST_MODE 什么时候开

`FAST_MODE=1` 同时降级三处：**追问 0 轮、ReAct 步数上限降到 2、残差辨证整体关闭**
（`core/followup.py::fast_mode_enabled()` 是全项目唯一判定实现，三处同时生效，
不会出现「关了一半」）。

- **对外演示一律开**——理由是上面 replay 限制第 2 条（追问内容一变就未命中），
  不是为了快
- 网络慢、或者时间紧时也开
- 实测代价与收益：一次完整问诊 **14 次调用 → 8 次**（这个数在 `README.md` 的开关表里）；
  耗时 **247s → 113s**（⏳ 这一组是项目方在真机上测的，仓库里没有文件可核，
  引用时带上这句）

默认关。让追问/ReAct 走真实路径更有说服力，那也是这个 demo 的卖点之一。

## 时间不够时砍哪几点

按「砍掉损失最小」排：**7 → 3 → 2 → 5**。7 在 replay 下本来就跑不了；3 和 2 是同一张图
上的细节，讲一个带过另一个；5 可以口头说「患者模式拿不到方药」而不真切。
**1、4、6 不能砍**——它们分别是这个 demo 的主张（分歧可量化）、最有冲击力的一段
（处方安全闭环）、和最该被记住的一条设计（拦截在推理之前）。

还有一条可选的第 8 点（20s，录过，主诉 `胸闷气短`）：信息太少时系统返回
**insufficient**，承认自己判断不了，而不是硬凑一个证型出来。时间宽裕、
或者评审问到「信息不全怎么办」时用。

## 第 4 步：检索三路融合（K3a/K3b，命令行演示，1 分钟）

```bash
python -m offline.build_jieba_dict
python -m offline.build_element_index   # 需要 cases.json + data/graph.json
python -c "
from core.retrieval import get_retriever
r = get_retriever()
for mode in ['dense', 'bm25', 'graph', 'hybrid']:
    try:
        hits = r.search('胃脘胀痛，嗳气泛酸', 'ye_tianshi', k=1, mode=mode,
                         query_elements=['脾','气滞'] if mode in ('graph','hybrid') else None)
        print(mode, hits[0][0].case_id if hits else '(空)')
    except Exception as e:
        print(mode, '不可用：', e)
"
```

判据：`bm25`/`graph` 两个模式不需要下载 embedding 模型，任何有 `cases.json` 的环境都能跑（构造检索器要读它）；
`dense`/`hybrid` 需要网络下载 `BAAI/bge-small-zh-v1.5`——没有网络的环境这里
会报错，如实说明，不是这两个模式的代码有问题（`data/SOURCES.md` 第 19、
21 条有这台开发环境的真实验证记录）。

网页上也能直接切：问诊页「检索模式」下拉框，或者请求体里带 `retriever_mode`。
**这是逐请求的，不是服务端全局设置**——两个人同时用同一个服务、各选一种模式
互不影响。可以现场演示两条失败路径的区别：

```bash
# 模式名写错 → 立刻 400，一次 LLM 调用都不花
curl -s -X POST http://127.0.0.1:8000/api/consult -H "Content-Type: application/json" \
  -d '{"complaint": "纳差乏力", "retriever_mode": "没有这个模式"}'

# 模式认识但这台机器缺数据（graph 缺 element_index.json）→ 200 + 一句人话，
# 不是 500，也不会偷偷换个模式跑完假装成功
curl -s -X POST http://127.0.0.1:8000/api/consult -H "Content-Type: application/json" \
  -d '{"complaint": "纳差乏力", "retriever_mode": "graph"}'
```

第二条的重点是**没有静默降级**：降级的话演示对象会以为自己看到的是证素路的
结果，E8 消融那组数字也就失去意义了。

## 第 4.5 步：图谱浏览器页签（1 分钟）

切到页面顶部的「图谱浏览器」页签。初始只铺 17 个证型节点，点开才逐步展开
连着的证素/症状；能按名字搜索，能按医家切换 λ1 权重（边的透明度按 λ1 编码）。

演示重点是**顶部那段 λ1 说明**：它按当前这张图的实际内容动态生成，两种成因
说的是不同的话——图里根本没挂医案是一种，挂了医案但医案证型跟国标术语对不上
是另一种。后者是这个项目一个真实的负面发现（839 条医案仅 91 条标注证型、
其中 0 条匹配国标证候名；那次测量时的语料是 839 条，现在是 941 诊次，
**λ1 没有在新语料上重测**），**不藏**。如果医案层是空的，"国标层/医案层"
切换按钮直接不显示，而不是显示一个点了没反应的。

## 第 5 步：噪声地板 ε（命令行，3-5 分钟，消耗真实 LLM 调用）

```bash
python -m offline.estimate_epsilon --dry-run          # 先看预估调用数
python -m offline.estimate_epsilon --n-repeats 3       # 真的跑，写 eval/epsilon.json
```

判据：`eval/epsilon.json` 生成后，第 1 步前端的分歧度会自动带上这个 ε 做
对照——这是「任何数字都必须带对照」这条项目硬约束在 UI 上的落地。

## 第 6 步：证素轨迹（附属功能，命令行，1 分钟）

```bash
python -m offline.quota                                # 先看样本量够不够门槛
curl http://localhost:8000/api/trajectories/ye_tianshi | python -m json.tool
```

判据：只有带复诊序列（同一 `case_group_id` 下 ≥2 诊）的病人会出现在结果里；
`data/element_index.json` 没生成时端点返回 503（不是 500，前端能区分"数据
没准备好"和"系统故障"）。**这一步只展示轨迹，不预测下一诊会怎么转移**
——样本量不够，拟合转移模型只会制造一个看着像结论的噪声数字，故意不做。

## 第 7 步：评测汇总 + 盲评（命令行，5+ 分钟，消耗真实 LLM 调用）

```bash
python -m eval.run_eval --queries-path tests/queries.txt
cat eval/report.md
```

判据：报告里每个数字旁边都带着对照基准（ε、有无参考医案分组、否决/正常
两组调用数、McNemar p 值）——`build_report()`/`render_markdown()` 的设计
就是不允许产出一个没有对照的数字。报告顶部还有一行 `**后端**：…`（模型 + 后端 +
是不是回放），**一份报告里混了两个后端会在那一行大声报出来**。

各组数的当前值、对照和 caveat 只维护在 [`eval/RESULTS.md`](eval/RESULTS.md) 一处，
这份 DEMO **不另维护一份**。讲解里必须出现的那几个数（第 1 点的 ε、第 6 点的
安全代价、第 7 点的检索差异率）**每一个都带凭据记号**，跟 RESULTS.md 和 README
走同一个核对器——R19 之前它们是手抄的，而这份文档自己声称"不复制数字"，
两件事对不上。要当场证明三份文档都没有手抄错：

```bash
python -m scripts.collect_results --check     # 退出码 0 = RESULTS.md 引用的数跟文件一致
```

盲评（人工评分，跳过实际填写只演示流程）：

```bash
python -m eval.mes.export --limit 3
cat eval/mes/items.json   # 展示：只有 A/B，看不出哪个是叶天士哪个是吴鞠通
```

## 第 8 步：外部评测 TCMEval-SDT（如果有数据集，命令行，视规模而定）

```bash
export SDT=<TCMEval-SDT 数据集路径>
python -m eval.sdt.run --sdt-dir $SDT --split Test --solver chain --out out/sdt_chain.txt
```

见 [`eval/sdt/README.md`](eval/sdt/README.md)——这是外部数据集、外部计分
标准，跟第 7 步的 V1（本项目自定义指标）是两套独立的评测，不要混着报数。

## 讲清楚"这个 demo 做不到什么"（演示末尾，1 分钟）

演示到这里之后，主动说明这些边界（见 `README.md`「不能做」一节），
不要等评审自己发现：

- 样本量各 30 案，不足以支撑统计结论，仅够验证系统管线
- 安全拦截关键词表未经医学生审核，可能漏报/误报
- λ1（医家层图谱权重）恒为 0——古籍证型跟国标术语体系基本不相交，
  是数据的客观性质，不是代码缺陷
- 这台环境的具体网络限制（连不上 huggingface hub）导致 dense/hybrid
  检索的真实数字这台环境产不出来，代码路径已验证、真实数字待 AutoDL
- 医生模式的处方安全表（十八反十九畏、剂量上限、必要煎法、毒性分层）
  方法论上是 WebSearch 核实，不是逐字核对药典原文——见 `README.md`
  「处方安全」一节和 `core/safety_output.py` 文件开头的方法论说明。
  这套检查能拦住明显的配伍禁忌和超量，**不能替代药师/医师本人的复核**，
  这也是为什么导出的是"建议稿"而不是可以直接执行的医嘱
