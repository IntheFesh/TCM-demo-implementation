# 更新日志

写给**使用者**，不是 commit 日志：每条说的是"你能看到什么变化"，
不是"改了哪个函数"。内部轮次编号（R40、R47 之类）不出现在这里，
它们在 `docs/reports/` 里。

版本号规则见 `core/version.py`：`主.次.修订`，正式验收通过前带 `-rc.N`。

---

## 1.0.0-rc.1

**第一个可交付评审的版本。** 与此前的内部版本相比：

### 界面

- 全站改为**正式版形态**：不再显示额度、API key 输入、检索方式选择、
  运行清单等内部信息。研究与评测功能仍在系统内，由运维通过配置开启。
- 结论页改为**一条自上而下的推导链**（症状 → 脏腑病位 → 病性 → 证型 →
  病机 → 治则 → 治法 → 方剂 → 君臣佐使），链上每个节点可点开查看它的
  依据与原文出处。
- 新增**首次使用引导**（三步：输入什么 → 看什么 → 怎么用），可跳过，
  也可随时从页脚重新打开。
- 复杂区域增加「?」说明入口，点开是一句话说明加一个例子。
- 等待时显示**骨架与预计剩余时间**，预计值来自本机此前几次问诊的实际历时，
  样本不足时如实说明而不给一个估计数。
- 输入超出语料覆盖范围时，明确说明当前覆盖什么、建议如何补充，
  不再是一个空白页面。
- 页脚固定显示用途与免责声明、版本号、本次记录编号（报障时提供给运维）。

### 内容

- 结论下方给出**本方的依据强度**一句话：本方有多少比例的药味可在本草与
  方剂本体中查到原文出处、另引了多少条医案与多少家名老中医经验。
- 未装载本草与方剂本体的部署会**明说"无从核对"**，不会显示成"查不到出处"。
- 措辞按医疗软件合规要求整体调整：系统陈述的是教材与医案中的记载，
  不对具体患者给出用药建议。危重表现的就医指引不受此影响，且更醒目。

### 兼容与部署

- 新增启动自检脚本，缺数据或缺配置时明确指出缺哪一项并拒绝启动。
- 打印输出按 A4 版式排版，页眉页脚与分页可直接用于打印处方笺与病历。
- 1366×768（诊室常见）与 768 像素宽（平板）下界面完整可用。
