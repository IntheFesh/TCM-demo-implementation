"""证型/门类名称的同义词归一化表。全项目只此一张表——医案门类关键词
（肿胀/木乘土/泄泻/胃痛…）跟证候定义文本之间"这其实是同一个概念"的判断，
和未来自由文本证型归一化到 core/elements.py 证素集合，都从这张表取数据。
两套独立写的归一化逻辑迟早会分叉、以后改一边另一边不知道，所以只建一处。

SYNONYMS 现在没覆盖到的门类词/写法，直接往这张表里加条目，不要在调用方
（build_graph.py 的覆盖检查等）里另开一套字面匹配规则绕开它。
"""
from __future__ import annotations

# 变体写法 -> canonical 概念名。canonical 名本身也作为自己的变体收在表里，
# 这样"文本包含 canonical 名"和"文本包含某个变体写法"走的是同一条查找路径。
SYNONYMS: dict[str, str] = {
    # 肿胀（含水肿类描述——两者在这批门类划分里是同一个证候范畴）
    "肿胀": "肿胀",
    "水肿": "肿胀",
    # 木乘土（肝脾关系，古称有两种方向性写法，指的是同一病机门类）
    "木乘土": "木乘土",
    "木旺乘土": "木乘土",
    "土虚木乘": "木乘土",
    "木不疏土": "木乘土",
    # 泄泻
    "泄泻": "泄泻",
    "大便溏稀": "泄泻",
    "大便稀溏": "泄泻",
    # 胃痛（"胃脘痛"是叶天士的门类名，"胃痛"是吴鞠通的门类名，同一概念）
    "胃痛": "胃痛",
    "胃脘痛": "胃痛",
    "胃脘隐痛": "胃痛",
    "胃脘疼痛": "胃痛",
    "胃脘胀痛": "胃痛",
    # 呕吐
    "呕吐": "呕吐",
    # 痰饮
    "痰饮": "痰饮",
    # 噎膈反胃
    "噎膈反胃": "噎膈反胃",
    "噎膈": "噎膈反胃",
    "反胃": "噎膈反胃",
    # 积聚（"积证"是同一病程分期描述里的简称写法，见气滞血阻证/瘀血内结证——
    # 两条证候定义里写的是"属积证初期/中期"，不是"积聚"这两个字本身）
    "积聚": "积聚",
    "癥瘕": "积聚",
    "积证": "积聚",
    # 吐血/呕血（消化道出血，对应 tests/queries.txt 安全否决那条测试主诉）
    "吐血": "吐血",
    "呕血": "吐血",
    "便血": "便血",
    "痞": "痞",
    "脾胃": "脾胃",
}


def canonical(term: str) -> str:
    """一个词条本身可能就是变体写法（比如覆盖检查传进来的 gate 关键词），
    先查表转成 canonical 名；表里没有就原样返回（当作它自己的 canonical 名）。"""
    return SYNONYMS.get(term, term)


def normalize(text: str) -> set[str]:
    """在 text 里找出命中了 SYNONYMS 表里的哪些 canonical 概念（任意变体写法命中
    即算）。不切分 text、不要求变体独立成词，就是子串匹配——但只需要维护一张表，
    比在多处各写一套字面规则要好审计、好扩展。"""
    hits = set()
    for variant, canon in SYNONYMS.items():
        if variant in text:
            hits.add(canon)
    return hits
