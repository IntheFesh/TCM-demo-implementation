# 项目约定

## 项目是什么

中医「名医辨证对照」demo。输入患者症状，系统分别模拟两位古代名医
（叶天士 `ye_tianshi`、吴鞠通 `wu_jutong`）的辨证思路，各自给出证型、
治法、方药，并把两者并置对比、标出分歧。每条结论必须引用它所依据的
真实医案 id（防幻觉）。

这是一个 walking skeleton：架构按正式版设计，内容填得少，后续只加厚不重写。

## 架构原则

- 这是 walking skeleton。所有实现都要能被替换而不改调用方。
  判据：将来换实现时，改的是一个类还是整个模块？必须是前者。
- 不要过度设计。demo 阶段不要加缓存、不要加数据库、不要加日志框架、
  不要加依赖注入容器。用最直接的写法。

## 已知的坑（必须避开）

- prompt 模板一律用 `string.Template`（`$var`），禁止用 `str.format()`。
  原因：prompt 里含 JSON 示例的花括号，`.format()` 会抛异常。
- LLM 返回可能带 markdown 围栏，解析前必须剥离。
- LLM 调用必须有重试（最多 2 次），第 2 次要把校验错误信息回灌给模型。
- 加载模型/大文件的对象一律惰性初始化，禁止在模块顶层实例化。
- S1（症状标准化）全局只跑一次，两位医家共用结果。如果对每位医家各跑
  一次，两次输出的症状列表会不同，后面构图时症状节点 id 对不上，边会
  指向不存在的节点。

## 代码风格

- Python 3.10+，用 `| None` 而不是 `Optional`
- 所有 LLM 输出都用 pydantic 模型承接，不裸用 dict
- 注释用中文，只写"为什么"不写"是什么"

## 数据与医家

- `data/ye_tianshi/*.txt`：叶天士医案，出自《临证指南医案》，公有领域
- `data/wu_jutong/*.txt`：吴鞠通医案，出自《吴鞠通医案》，公有领域
- 医案来源与版权说明见 `data/SOURCES.md`
- physician id 全部小写下划线：`ye_tianshi` / `wu_jutong`，不要用拼音以外的写法

## 目录结构

```
core/      数据模型、LLM 抽象层、检索、推理链——全项目地基
offline/   离线脚本：医案抽取、SFT 样本导出
api/       FastAPI 服务
web/       前端单页（无构建步骤）
prompts/v1/ 版本化 prompt 模板（yaml）
data/      医案原文与 SOURCES.md
tests/     pytest 用例与测试主诉
```

## 改造期新增约定

以下五条是 `feat/divergence-v1` 改造引入的，后续每轮都要遵守。

### 数据形态变了

医案不再是单诊快照。`CaseRecord` 会带 `case_group_id` / `visit_index` /
`prev_case_id`，同一病人的多次复诊是一个序列。检索、SFT 导出、图构造都要
意识到这一点。

### 任何数字都必须带对照

"分歧度""准确率""幻觉率"这类数字，禁止单独出现在 README、前端或报告里。
每个数字旁边必须有它的对照基准（对照组的数、或噪声地板 ε）。
一个没有基准的数字在这个项目里等于没有意义。

### 防幻觉约束不许放松

`schemas.py` 里所有 `Field(min_length=1)` 都不许改成可选、不许改成
`min_length=0`。如果某个新场景导致它校验失败，正确做法是**新建一个不含该
字段的 schema**，不是放松原来的约束。这条如果被违反，整个防幻觉设计就废了。

### 安全否决在 S2 之前

危重症状的拦截必须发生在证素推断之前，不是在最后的 `note` 里提一句。
被拦截的请求不产出任何方药。

### 追问的回答必须先过 check_safety

追问是安全否决层的后门。S2 之前拦的是初始主诉，如果追问问出了危重症状
（「有没有便血」→「有」），回答直接进证素推断、系统继续开方，那道拦截就被
绕过去了。所以：**追问返回的回答必须先过 `check_safety`，命中即按拦截处理，
不能直接进 S2 更新后验。**

`core/tools.py` 的 `question_candidates` 会给每个候选带 `safety_relevant`
标记（判据复用 `core/safety.py` 的表，不另建），提问方据此决定回答要不要
先送安全层。这条跟「安全否决在 S2 之前」是同一条约束的两个入口，缺一个
等于没有。

### 同一概念的匹配逻辑只能有一处实现

新增工具或新增判断时，先查有没有现成的匹配器，有就复用，不要另写一套字面匹配。

这个项目已经在同一堵墙上撞了三次：

1. 覆盖检查用字面子串比「水肿」和「肿胀」，把等价写法全判成未覆盖
   （修法：统一走 `core/syndrome_norm.py` 的 SYNONYMS）
2. 分歧度用字符串相等比证型名，把治法方药一字不差的两条判成分歧
   （修法：改用药物集合的 Jaccard 距离）
3. `query_graph` 查「口苦」失败，而同一模块的 `check_residual` 能匹配上
   ——两个工具对同一个词给出相反答案
   （修法：`query_graph` 复用 `_match_graph_symptoms`）

第三次这类最难查：每个工具单独测都正常，只有把它们放进同一条推理链里
才看得出矛盾。所以判据是「这个判断此前有没有人做过」，不是「我这个实现
有没有 bug」。

例外只有一种：两处回答的**不是同一个问题**。`core/safety.py` 的危重关键词表
就刻意不并进 SYNONYMS——那张表回答「这个词属于哪个证候门类」，安全表回答
「要不要在辨证开始前拦截整个请求」，合并成一张，以后改一边会看不出会不会
连带影响另一边。走这条例外时必须在代码里写清楚两个问题的区别。

### 评测代码放 eval/，不放 tests/

`tests/` 必须保持"不需要网络、不需要 API key、秒级跑完"的性质。
需要真实 LLM 调用的评测一律放 `eval/`，单独运行。
