# 项目约定

## 项目是什么

中医「名医辨证对照」demo。输入患者症状，系统分别模拟两位古代名医
（叶天士 `ye_tianshi`、吴鞠通 `wu_jutong`）的辨证思路，各自给出证型、
治法、方药，并把两者并置对比、标出分歧。每条结论必须引用它所依据的
真实医案 id（防幻觉）。

这是一个 walking skeleton：架构按正式版设计，内容填得少，后续只加厚不重写。

## 架构原则

- 这是 walking skeleton。所有实现都要能被替换而不改调用方。
  判据：将来换实现时，改的是一个类还是整个模块？必须是前者。
- 不要过度设计。demo 阶段不要加缓存、不要加数据库、不要加日志框架、
  不要加依赖注入容器。用最直接的写法。

## 已知的坑（必须避开）

- prompt 模板一律用 `string.Template`（`$var`），禁止用 `str.format()`。
  原因：prompt 里含 JSON 示例的花括号，`.format()` 会抛异常。
- LLM 返回可能带 markdown 围栏，解析前必须剥离。
- LLM 调用必须有重试（最多 2 次），第 2 次要把校验错误信息回灌给模型。
- 加载模型/大文件的对象一律惰性初始化，禁止在模块顶层实例化。
- S1（症状标准化）全局只跑一次，两位医家共用结果。如果对每位医家各跑
  一次，两次输出的症状列表会不同，后面构图时症状节点 id 对不上，边会
  指向不存在的节点。

## 代码风格

- Python 3.10+，用 `| None` 而不是 `Optional`
- 所有 LLM 输出都用 pydantic 模型承接，不裸用 dict
- 注释用中文，只写"为什么"不写"是什么"

## 数据与医家

- `data/ye_tianshi/*.txt`：叶天士医案，出自《临证指南医案》，公有领域
- `data/wu_jutong/*.txt`：吴鞠通医案，出自《吴鞠通医案》，公有领域
- 医案来源与版权说明见 `data/SOURCES.md`
- physician id 全部小写下划线：`ye_tianshi` / `wu_jutong`，不要用拼音以外的写法

## 目录结构

```
core/      数据模型、LLM 抽象层、检索、推理链——全项目地基
offline/   离线脚本：医案抽取、SFT 样本导出
api/       FastAPI 服务
web/       前端单页（无构建步骤）
prompts/v1/ 版本化 prompt 模板（yaml）
data/      医案原文与 SOURCES.md
tests/     pytest 用例与测试主诉
```
